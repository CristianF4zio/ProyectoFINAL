// import { useContext, useEffect, useState } from "react";
// import { AuthContext, useAuth } from "../../Context/contex";



export function Profile() {
    // const { user } = useAuth(); // Obtén el usuario actual del contexto de autenticación
    // const [profileImage, setProfileImage] = useState<string | null>(null); // Estado para almacenar la URL de la imagen de perfil
    
    // // Ejecuta el efecto cada vez que el usuario cambie
    
    return (
        <div>
            {/* {profileImage ? (
                <img src={user.getIcon} alt="Profile" />
            ) : (
                <p>No se encontró ninguna imagen de perfil para este usuario.</p>
            )} */}
        </div>
    
 ) 
}